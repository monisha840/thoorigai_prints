/**
 * Non-component 3D infrastructure. The scene components that consume it live
 * in `src/components/three/`.
 */
export {
  canvasDefaults,
  cameraPresets,
  lightRig,
  materials,
  floatDefaults,
} from './config';

export type { CameraPreset } from './config';
